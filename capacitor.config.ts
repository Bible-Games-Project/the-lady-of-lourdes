import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.biblegamesproject.ourladyoflourdes",
  appName: "The Lady of Lourdes",
  webDir: "dist",
};

export default config;
