{
  "name": "the-lady-of-lourdes",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}